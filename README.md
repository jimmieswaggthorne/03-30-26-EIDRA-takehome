# Interviews workspace

This repository contains:

- **`web/`** — Next.js restaurant discovery UI ([setup](web/README.md))
- **`proxy/`** — local API proxy for the upstream restaurant service

Run the app: start **`proxy`** then **`web`** (see `web/README.md`).
